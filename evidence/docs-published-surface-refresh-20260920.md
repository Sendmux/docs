# Published-surface fact refresh — 20 September 2026

Scope: bring PR #56 up to date with `main`, refresh the facts it asserts against the
registries that publish them, and record the SDK release in the changelog.

## Merge

`origin/main` merged into `agent/mcp-oauth-docs` (merge commit `dc0ee09`). One conflict,
`changelog.mdx`, where both sides prepended entries. Resolved newest-first: main's
19/19/18 September entries, then this branch's two approved 17 September entries, then
main's two 17 September entries.

Preservation check, run over the resolved file against both parents:

```
branch=108 main=111 resolved=113
branch entries NOT byte-identical in resolved: 0
main entries NOT byte-identical in resolved:   0
union preserved: True | resolved unique: 113
```

Every `<Update>` block from both sides survives byte-for-byte. No approved unit was
rewritten and no humanisation was re-run. `<Update>` open/close tags balance at 113/113
with strict alternation.

## MCP release facts

`snippets/mcp-release-facts.mdx` is generated by `scripts/check-mcp-docs-contract.mjs`
from a pinned `sendmux-sdk` commit, so the fix is a pin bump plus a regen, not a hand edit.

- Pin moved `8a204d91eab96652ec39e9e59cef6280ef88b925` (declares `sendmux-mcp` 2.1.0)
  → `9077b5668ac762971e4670270d6f6b38d2af7ab1` (declares 2.1.2).
- Regenerated with `npm run mcp-docs:emit`. Only the rendered package version moves;
  identity, hosted resource, transport, curated tool count (54) and both protocol lists
  are unchanged at the new pin.

Verified from the registries, not from memory:

```
pypi.org/pypi/sendmux-mcp/json                          -> 2.1.2
registry.modelcontextprotocol.io/v0/servers?search=sendmux
  -> io.github.Sendmux/sendmux-mcp 2.1.2, status active,
     published 2026-09-19T12:10:07Z,
     remote streamable-http https://mcp.sendmux.ai/mcp,
     package sendmux-mcp 2.1.2 (pypi)
mcp.sendmux.ai/health                                   -> {"status":"ok", ...}
mcp.sendmux.ai/.well-known/oauth-protected-resource     -> resource https://mcp.sendmux.ai/mcp
```

The hosted server's own `serverInfo.version` could not be read directly: `initialize`
returns 401 with an OAuth challenge. The MCP Registry entry and PyPI are the registry
evidence used instead.

## Changelog entry

New `20 September 2026` entry for the coordinated SDK majors, code-sourced from
`Sendmux/sendmux-sdk`:

- `packages/ts/management/CHANGELOG.md` 2.0.0 — `managementListProviders` returns
  `ProviderListItem` without `variables`; `managementGetProvider` returns `ProviderItem`
  with required `variables`.
- `packages/python/management/CHANGELOG.md` 2.0.0 — combined provider variables and
  delivery routing (`81bbf22`).
- `packages/ruby/sdk/CHANGELOG.md` 2.0.0 — `management_get_provider` returns
  `ProviderItem` with a required `variables` hash.
- `go/CHANGELOG.md` 2.0.0 — module path `sendmux.ai/go/v2` (tag `go/v2.0.0`);
  `ProviderItem.Variables` added; `EmailSendRequest.DeliveryGroup` narrows the eligible
  provider pool for one send.

Disposition `ineligible-manual` at 44 eligible words, below the 50-word submission floor;
source preserved byte-identically. Ledger:
`automation/ledgers/2026-09-20-sdk-account-variables-and-routing.json`.

Distribution channels are deliberately absent from the entry. The Homebrew tap formula is
still 1.6.0 with its 1.7.1 PR unmerged, and Chocolatey lists only 1.6.0 with 1.7.1 awaiting
moderation, so naming either would have dated the entry.

## Facts checked and left unchanged

- `developer-tools/sdks/php.mdx` — `composer require sendmux/*:^2.1` matches Packagist
  (`sendmux/mailbox` 2.1.0, `management` 2.1.1, `sending` 2.1.1, `core` 2.1.1, `sdk` 2.1.0).
- Every other install line across the guides is unversioned (`pip install sendmux-mcp`,
  `npm install -g @sendmux/cli`, `gem install …`, `cargo add sendmux`), so no version can
  go stale.
- `developer-tools/cli.mdx` names Snap (1.7.1 live on stable and edge) and Homebrew
  (1.6.0 live) without versions; both claims hold. Chocolatey is not mentioned anywhere in
  the docs, which is correct while 1.7.1 is unapproved.
- `developer-tools/sdks/go.mdx` documents the v1 module (`go get sendmux.ai/go@latest`).
  v1.6.1 is still published and not retracted, so the page is not wrong, but it does not
  yet cover `sendmux.ai/go/v2` v2.0.0. Migrating that page is a separate change: it means
  rewriting every import path in its code samples against the v2 API.

## Gates

```
npm test                  exit 0   (37 pass, 0 fail)
npm run mcp-docs:check    exit 0
npm run confidentiality:check  exit 0
npm run external-links:check   exit 0
npm run postman:check     exit 0
```

CI `Docs gates` green and CodeRabbit green on the pushed head. All four earlier review
threads on PR #56 are resolved.

Raw captures: `sendmux-sdk/.claude/artifacts/l9/`.
